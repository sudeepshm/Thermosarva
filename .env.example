# ── Thermosarva Frontend Environment Configuration ───────────────────────────
# In development: leave blank or omit to use Vite's local dev proxy (/api -> http://localhost:8000)
# In production (Vercel, Render, Netlify): set to your deployed backend URL + /api
VITE_API_BASE_URL=https://thermosarva-backend.onrender.com/api
